# Opportunity round: close-out

Branch `opportunity-round`, off `main` at `1124917`, which was confirmed equal
to `origin/main` and to `git ls-remote origin main` before the branch was cut.
**Nothing merged, nothing pushed.**

---

## 1. The rulings, and where each landed

| | Ruling | Landed |
|---|---|---|
| **Step 0** | The pre-push hook, mechanical enforcement for build discipline 18 | Its own commit, and rule 18 amended to record it built |
| **R1** | Part A mounts as a React island, third caller of the `contact/` components | The region commit |
| **R2** | The title renders again, after listing what else died with the retirement | The region commit |
| **R3** | The follow-up keys join the Opportunity, one mechanism | Its own commit |
| **R4** | The red is DERIVED before adoption | **Derived and STOPPED at its colour word. Not committed** |
| **R5** | `Record scores` wears the estate's treatment | Its own commit, cosmetic tier |
| **R6** | The headline strip's 1240 wrap, inside part A's region | The region commit |

---

## 2. What Phase 0 changed about the round

**Two premises in the instruction were corrected by measurement**, and both
were mine to check rather than John's to have got right.

- **"23 opportunities" is 18.** The zero carrying a follow-up was correct and
  calibrated against contacts, which show two.
- **Part A was not a caller change.** There is no `opportunity/` module in
  React at all; that surface is vanilla `app.js`. R1 ruled the island route on
  that finding, which is the finding doing its job.

**And Phase 0's own first probe printed `1000 total, 6 live`**, which is
PostgREST's default cap and therefore a truncated page rather than a count.
Both numbers were wrong and the 6 would have been reported as the population.

---

## 3. The retirement's full cost, which R2 asked for first

`opportunity-reference.js` addressed 29 ids. Six were live with no writer, and
the sixth item is not an id at all:

**89 note entries across 11 of 18 live opportunities had been rendered by
nothing since the retirement.** No React card replaced `ref-notes-list`, and a
census of what a surface RENDERS cannot see that, because what renders is
correct. The capability was the thing that died.

That is why part A's Notes card is a restoration rather than a feature.

---

## 4. Guards that caught the round

- **The estate's own `no-duplicate-ids` suite REFUSED a commit**, because its
  calibration anchor was `ref-save-all`, the dead button this round removed. Its
  failure message said "re-derive the rule" and it was re-derived as a synthetic
  parser check no later round can delete.
- **The pre-push hook refused a real push**, with zero refs landing, against a
  throwaway local bare repo.
- **The red census REFUSED to report a count** on inputs it could not classify,
  and caught HTML numeric entities being read as colours.
- **The journal guard refused an ambiguous edit anchor** twice.

---

## 5. What the screenshots found that assertions could not

**Twice**, and both times every programmatic check was green.

- The region probe's first three captures read **"Loading the record..."** under
  48 passing assertions, because `.is-loading > *` is `visibility: hidden`,
  which PRESERVES LAYOUT. Every child kept a correct non-zero box while
  invisible, so a wait on rendered text passed and a guard asserting the tab row
  was on screen passed.
- **`cd-followup-save` was a white browser default**, found by opening the owned
  record's capture. No assertion in the round was looking at it.

---

## 6. Carried

1. **R4's adoption**, at its colour stop.
2. **`#ref-vanilla`** still carries eleven of the retired file's ids.
3. **`ref-save-feedback`** is an id in the vanilla markup and a `data-testid` in
   React: two things, one name, one surface.
4. **`.tab-action-idle`** now appears only in `style.css` and in no live script.

---

## 7. The commits

| commit | what | gate |
|---|---|---|
| `10b873e` | the pre-push hook, its test, and rule 18's amendment | own suites |
| `bc0f887` | the brief | markdown only, rode `10b873e` |
| `6b499d1` | Phase 0: four answers and the top-region defect | own suites |
| `1cced67` | R3, the follow-up task as one mechanism | own suites |
| `1f191ac` | the region: R2, R6 and part A | own suites |
| `b14205c` | R5, two save controls | own suites |
| this one | the close-out and `CURRENT_STATE.md` | the round gate below |

**One commit rode a preceding green gate**, named here as build discipline
48(a) requires: `bc0f887`, the brief, which is markdown that no gate stage
reads.

---

## 8. `CURRENT_STATE.md`, regenerated and reconciled

- **live stays 132**, so no fixture from this round survived. Every probe that
  created one tore it down and re-queried by tag.
- **soft-deleted grew**, which is what a soft delete leaves behind and is this
  round's fixtures.
- **two tag row counts moved** by this branch's commit count.
- **no opportunity status count changed**, which is the check that matters for
  a round that created and destroyed opportunities: 5 Qualification, 3
  Proposal, 7 Solution Alignment, 0 Evaluation, before and after.

Every line of the diff is accounted for by a phase of this round.

---

## 9. Exit gate

| Point | Answer |
|---|---|
| Every ruling built or recorded | R1, R2, R3, R5, R6 built; **R4 derived and STOPPED at its colour word** |
| Rulings appended at the phase they launched | Yes, in the brief and in each commit message |
| Every new guard calibrated both directions | pre-push 5/5, R3's invariant 2/2, the region 3/3, the red census 5/5, R5 2/2, the re-derived id anchor 2/2 |
| Live proof at both widths | 48/48 at 1440 and 1240, two records plus an owned fixture |
| Read back from the database | Yes: R3's route, and the band's own note and follow-up |
| Screenshots opened and read | Yes, and they found two defects assertions could not |
| Superseded callers dispositioned | Yes, 29 ids each classified |
| Fixtures torn down | Yes, re-queried by tag, live unchanged at 132 |
| `CURRENT_STATE.md` regenerated and reconciled | Section 8 |
| Full gate | Recorded in the round report |
| Merged or pushed | **No** |
