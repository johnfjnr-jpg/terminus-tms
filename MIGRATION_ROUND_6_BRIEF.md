# Migration Round 6: the retirement debt, then Contact

**Final, 2026-09-06.** Ruled by John this date: Contact is Round 6's
surface; Test Bed then app.js follow. Context recorded with the ruling:
there are no live deployments and all data is test data, so no surface
carries operational urgency and the ordering is decided purely on
rehearsal value - Contact exercises the whole-view pattern with proven
editors before Test Bed takes the second real door at larger scale.

Two acts. Act one pays the retirement debt with designed work: the ~31
assertions that must exist before `opportunity-deal.js` can die,
derived from contracts and written vanilla-free so
`opportunity-reference.js` (due at this round's close) retires
mechanically behind them. Act two migrates Contact: 1,327 lines, a
whole-view surface on the Account pattern, no ownership door (the
Account preserve ruling's precedent applies), the Reference draft-store
shape, a link-account panel, a revision handshake, and the Qualify
workflow with its blocking[] validation, which is business machinery
the census enumerates as behaviour.

Method per the skill and all promotions through Round 5, including:
retirement scope sized by sandbox deletion, never mention count; the
snapshot-delete-restore rehearsal; census on initialised exercised
surfaces; node-stability typing detectors; computed visibility over
attribute presence.

---

## Phase R: the retirement session (act one, before the surface)

1. Sandbox-delete `opportunity-deal.js` and enumerate the failures
   (the 31, re-measured on the current tree). For each: the CLAIM it
   carried, then its disposition - already covered by the React
   corpus (delete with reasoning), claim died with the file (delete
   with reasoning), or claim survives and needs a new assertion.
2. New assertions designed from the contracts (the census, B1-B7, the
   seam rulings, the field-row contract), never from either
   implementation, reading NO vanilla file, red where feasible
   against deliberate breaks. This is Verification 47 work and is
   named as such.
3. opportunity-headline's three reference-file reads resolved the
   same way (the false-liveness shape live in the tree).
4. Then the retirements per policy, each its own commit, two-claims
   verified: opportunity-deal.js with the parity suite;
   opportunity-reference.js if its remaining asserters are already
   clean (measure; if not, it lands at Phase 3 with the round's
   close as the policy intends).
5. Gate green; the estate ledger updated with instruments.

## Phase 0: Contact investigation (no product code)

1. The view boundary: loadContactDetail, cdReturnView semantics (back
   to leads or contacts), markup extent, inline onclick references
   classified with instrument.
2. The field census, both instruments, on an initialised exercised
   record: CD_ALL_FIELDS against the DOM, per-field editor kind and
   contracts, read-only set, the name header pattern.
3. THE QUALIFY WORKFLOW enumerated as behaviours: the blocking[]
   list's lifecycle (set on refusal, cleared when), what qualifies,
   the routes, what renders where, the return-view interaction.
4. The link-account panel against Account's parent-link precedent:
   immediate save semantics, in-flight guard (cdLinkInFlight),
   search behaviour.
5. The save path: payload shape, dirty keys, revision handshake, 409
   sentence, change sentences (cdChangeSentence).
6. Coupled tests and probes with instrument, classified; strings
   scan; retirement preconditions for contact-detail.js itself
   (sized by sandbox deletion, recorded for Round 7).
7. Door check: measure that no ownership read exists; apply the
   Account preserve ruling by precedent, recorded (John may
   overrule if leads carry different ownership semantics).
8. Editor-slot fit: expected all proven layers; any gap named before
   Phase 1.

## Phase 1: behind the line

Rows through the proven component with census descriptors; the
Qualify machinery and link panel per their enumerations; first-contact
checklist run (fifth entry); injections per family including the
standing detectors (node stability, computed visibility, duplicate
ids); fixtures per the standing rules.

## Phase 2: the swap, whole in one session

The swap commit with re-points, ledger, strings scan, live-form
inversion; the primary write path walked live: every editor kind, the
batched save with 409, the Qualify workflow end to end including a
blocking refusal and its resolution, the link-account round-trip,
return-view both ways. Visual comparison, three widths, exercised
states, divergences deliberate-or-fixed.

## Phase 3: walk, revert, retirements, close-out

Full walk; revert rehearsal; any retirement deferred from Phase R
lands here; rule promotion check; CURRENT_STATE.md; close-out with
the estate ledger and Round 7 entry context (Test Bed with its real
door, then app.js with the declaration-keyword inventory, now
including the let/const list Round 5 confirmed).

## Exit gate for Round 7

1. The retirement debt is zero: deal form, parity suite and
   reference file gone, two-claims verified, no policy exceptions
   outstanding.
2. Contact's census rows and the Qualify workflow pass live; the
   checklist has written verdicts.
3. The estate ledger restates remaining coupled counts with
   instruments, and Test Bed's retirement precondition is sized by
   sandbox deletion.
