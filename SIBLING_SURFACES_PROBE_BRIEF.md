# The sibling surfaces probe round: brief

Governing docs: CLAUDE.md, the tms-round-method skill,
WRITE_AUTHORIZATION_BRIEF.md and its close-out. Drafted after the
write authorization round closed at 33a2053; re-verify premises
against the tree you are on. This brief's R-series is its own.

## Rulings of record (John, 2026-09-08)

R1. Scope: a PROBE-ONLY round. No source, schema or policy changes
    of any kind. Its deliverable is measurement: every Test Bed and
    Contact write path attempted as a non-owner, each refusal
    asserted with its reason, plus the owner counterfactual. If any
    non-owner write LANDS, STOP immediately and report with ids —
    that finding re-rules the round, as it did last time.
R2. Promotions into CLAUDE.md, one commit, first act (per the
    ruling that conversational rulings are appended at the phase
    they launch): (i) a revert rehearsal restores from an explicit
    ref, never the index, and verifies the tree hash rather than
    reading git status; (ii) rulings given in conversation are
    appended to the brief at the phase they launch, not discovered
    at the close.
R3. The seven artefacts the corrected residue counter surfaced are
    DELETED as a counted data change, first act after R2: the five
    versions and one request on records already soft-deleted by
    R10, and the withdrawn request of 2026-09-02 on walk65's live
    record 29e98c46. Ids before, per-id result, and the corrected
    counter re-run reading zero rows written by the probe account
    on records it does not own, anywhere.
R4. Method unchanged: stop for sign-off, final-act gate on the
    exact tree, the word follows the stated gate result, nothing
    pushes without it.

R5. The three create-from paths are ruled OWNER-ONLY: convert,
    create-test-bed and create-opportunity all require ownership of
    the SOURCE record. Approvers endorse, owners execute, and
    consuming a bed's conversion allowance and its reference code
    is execution. If colleague-initiated conversion is ever wanted
    it arrives as an explicit reassignment or roles feature in its
    own round. THE FIX IS A FIX ROUND, NOT THIS ONE.
R6. The probe RESUMES to complete the table: scores, units/:unitId,
    key-contacts and its DELETE, tech-team past its precondition,
    and PATCH /accounts/:id with a corrected body - expected MATCH
    as team-editable by design, and the design is to be proven
    rather than assumed. THE STOP RULE STAYS ARMED for any NEW
    landing; the three known landings are findings, not stops,
    from here.
R7. Evidence: the three created records (69947943, 745cd214,
    ee047aca) and the audit rows written onto the three SOURCE
    records are KEPT, listed by id, until the fix round calibrates
    against them. Their deletion is proposed at that round's close.

R8. The brief's R1 sentence claiming the map calls accounts
    team-editable is CORRECTED, struck in place above with the
    superseded wording left visible. It was the drafter's
    unverified recall, contradicted by the map (records UPDATE:
    ownership) and by measurement (PATCH /accounts/:id refuses a
    non-owner, ownership-shaped). THE MAP'S VERDICT STANDS.
R9. Unit ownership is QUEUED as a product question, and is neither
    this round's scope nor the fix round's: units are records
    owned by whoever derives them, so a handed-over bed leaves its
    units behind and a bed's owner may be unable to edit their own
    bed's units. PROVISIONAL DIRECTION for the future ruling: unit
    write rights follow the parent bed's owner.
R10. The round closes as a PROBE ROUND: eleven MATCH, three
     landings ruled owner-only under R5 and handed to the fix
     round, evidence kept under R7 for that round's calibration.

## Why this round exists

Every live proof in the write authorization round used an
Opportunity. The policies are shared, so the shapes carry — but
that is an argument, and that round began because a security claim
rested on one. This round converts the argument into measurement.

## Phase 0: the probe

1. The named paths, each as a non-owner with an owner
   counterfactual, refusal REASON asserted (ownership-shaped, not
   any 4xx — the reached-the-policy lesson applies: identical
   inputs can draw a no-change or gate refusal before ownership is
   ever asked, so construct each attempt to get past earlier
   gates):
   Test Bed: scores, measurability, buyer-contacts, tech-team,
   units/:unitId, PATCH /test-beds/:id, convert.
   Contact: PATCH /contacts/:id, link-account, create-test-bed,
   create-opportunity, the key-contact link and its DELETE.
   Account: PATCH /accounts/:id — ~~the map says accounts are
   team-editable by design~~ STRUCK, see R8. That sentence was the
   drafter's unverified recall. The map says records UPDATE is
   OWNERSHIP, an Account is a records row, and measurement agrees:
   a non-owner is refused 403 ownership-shaped. The superseded
   wording is left visible rather than deleted, so a reader can see
   that a premise failed rather than a preference changing.
   The instruction that survives is the useful half: prove the
   design rather than assume it. The round did, and the design is
   owner-only.
2. Sweep the route table for any write path on these surfaces the
   list above misses; the census instrument from the write
   authorization round is the tool. Every write route on Test Bed,
   Contact and Account is either probed or named with the reason
   it cannot be (with the gate that blocks it identified).
3. Fixtures follow the estate's convention: built the way the
   system produces the state, torn down at the end as a counted
   change, ids recorded. Any write that lands is kept as evidence
   under the standing rule and listed for disposition.
Stop for sign-off with the results table: path, non-owner result,
owner result, verdict.

## Phase 1: gate and close

No source changed, so the gate should be a formality — run it
anyway on the exact tree (the probe scripts are new files the
gate's source scans read). Reconcile by counting. The close states
what remains unexercised anywhere in the estate as a non-owner, so
the map's proof coverage is explicit from here on. Nothing pushes
without the word, and the word follows the stated gate result.

## After this round

The parked UI hygiene round resumes: the door work now describes
an enforcement that is proven on every surface the door guards,
not argued from shared shapes.
