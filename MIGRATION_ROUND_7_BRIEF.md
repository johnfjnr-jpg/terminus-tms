# Migration Round 7: Test Bed

**Final, 2026-09-06.** Sequencing ruled in Rounds 5 and 6: Test Bed is
the last surface; the shell round follows. No open decisions.

The estate's largest remaining surface: `frontend/test-bed-detail.js`,
3,260 lines, a whole-view migration carrying the second real ownership
door (read through the proven `CAN_EDIT_BY_VIEW` registry), the
Reference draft-store convention, a revision handshake, 25 globals of
the `window.X = function` kind, a debounced server-side cost preview (a
live-compute-on-edit pattern no migrated surface has needed), inter-date
bounds, and whatever else the accounting instrument finds - which is the
point of running it first.

Method per the skill and all promotions through Round 6, with these
named as mandatory: the accounting instrument before the census is
believed; the census on an initialised exercised record with both
instruments; the re-navigation harness on every view test; sequencing
guarantees re-proven under batching; retirement sized by sandbox
deletion; every fresh assertion presumed wrong until an injection has
failed it.

**Retirement at this round's close**, per policy: `contact-detail.js`
(sized at 4 in Round 6). Test Bed's own vanilla falls due at the shell
round's close.

---

## Phase 0: investigation (no product code)

1. THE ACCOUNTING INSTRUMENT FIRST: every top-level name including
   `window.X = function` mapped to an enumerated capability; unmapped
   names are findings; calibrated. The capability list, not the field
   list, is this surface's scope.
2. The field census, two instruments, on an initialised exercised
   record: `TB_ALL_EDITABLE_FIELDS`' nine groups against the DOM,
   per-field editor kind (four kinds plus any new), empty-state
   contracts, read-only set, the name header.
3. THE DOOR: every reader; what the :6520 toggle derives notMine
   from; both directions measured live using the not-owned fixture
   pattern (admin write); the registry line's state for this view.
4. THE COST PREVIEW enumerated as behaviours: the debounce, what
   triggers it, the server round-trip, what renders where, staleness
   between edit and response, what happens on refusal, and how it
   interacts with dirty tracking (a preview is not a save).
5. Date bounds (`refreshTbDateBounds`), sensor counts, buyer roles,
   the notes region, and every capability the accounting instrument
   lists - each enumerated as behaviours before build.
6. The save path: payload shape, dirty keys, `tbPatch`, the stale
   message, the 409 sentence.
7. Coupled tests and probes with instrument; sandbox-deletion sizing
   for test-bed-detail.js (recorded for the shell round); strings
   scan; the door's interaction with the ownership sweep at :6520
   noted for the shell inventory.
8. Editor-slot fit: expected proven layers plus whatever the cost
   preview needs; any gap is a dated addendum before Phase 1.

## Phase 1: behind the line

Rows through the proven component; the cost preview and every
enumerated capability built B-style, contract-derived tests red
first; the door wired through the registry, injection-calibrated both
directions in jsdom; first-contact checklist (sixth entry); the
standing detectors (node stability, computed visibility, duplicate
ids with dispositions, accounting); injections per family.

## Phase 2: the swap, whole in one session

The swap commit with re-points, ledger, strings scan, live-form
inversion, any C1-pattern shell adaptations measured in Phase 0; THE
PRIMARY WRITE PATH LIVE: every editor kind, the batched save with the
409 sentence, the cost preview round-trip (edit, debounce, server
figure rendered, refusal path), date bounds enforced, THE DOOR ON
REAL OWNERSHIP both directions (the not-owned fixture; every row and
capability refusing; the owned record opening everything). Visual
comparison, three widths, exercised states, different-implementations
asserted. Re-navigation walked (second visit works).

## Phase 3: walk, revert, retirement, close-out

Full walk; revert rehearsal (byte-identical verified, the checkout
hazard rule in force); contact-detail.js retired per policy,
two-claims verified; rule promotion check; CURRENT_STATE.md;
close-out against the exit gate with the estate ledger and THE SHELL
ROUND'S ENTRY CONTEXT in full: the declaration-keyword inventory
(let/const unreachables, function/var implicit globals, window.X
assignments), the C1-pattern seam items accumulated across rounds,
the ownership sweeps at :6520 and :8049, window.api and the shell
services seam's future, and test-bed-detail.js's sized retirement.

## Exit gate for the shell round

1. Test Bed's capabilities and door pass live in both directions;
   the checklist has written verdicts; second-visit behaviour
   proven.
2. Contact's vanilla is retired clean; the estate's only remaining
   vanilla surface file is test-bed-detail.js, sized.
3. The shell inventory is complete enough that the shell round's
   Phase 0 starts from a list, not a discovery.
